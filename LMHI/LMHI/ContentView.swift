import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("LMHI")
            .font(.title)
            .fontWeight(.bold)
            .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
