import SwiftUI

struct Default: View {
    var body: some View {
        Text("LMHI")
            .font(.title)
            .fontWeight(.bold)
            .padding()
    }
}

struct Default_Previews: PreviewProvider {
    static var previews: some View {
        Default()
    }
}
